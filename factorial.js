// Write code to create a function that returns the factorial of `num`

var factorial = function (num) {
  // Create empty array
  let list = [];

  // Push elements from 0 to num into the array
  for (var i = 0; i <= num; i++) {
    list.push(i);

    // Add all elements in the array

    console.log(list.length);
  }
};
